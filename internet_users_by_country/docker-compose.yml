version: '3'
services:
  streamlit:
    container_name: "streamlit-internet-users-by-country-app"
    build:
      dockerfile: ./Dockerfile
      context: ./
    environment:
      BASE_URL_PATH: "${BASE_URL_PATH}"
    ports:
      - '${EXTERNAL_PORT:-8502}:8501'